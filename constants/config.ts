export const APP_CONFIG = {
  // Default daily step goal
  DEFAULT_GOAL: 10000,

  // Goal range
  MIN_GOAL: 5000,
  MAX_GOAL: 20000,
  GOAL_STEP: 1000,

  // Calories per step (approximate)
  CALORIES_PER_STEP: 0.04,

  // Average step length in km (for fallback calculation)
  STEP_LENGTH_KM: 0.000762, // ~76.2 cm

  // Auto-save interval in milliseconds
  AUTO_SAVE_INTERVAL: 30000, // 30 seconds

  // App version
  VERSION: '1.0.0',
};
